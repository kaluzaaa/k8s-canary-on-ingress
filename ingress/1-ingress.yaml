apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: pets-cat
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: localhost
    http:
      paths:
      - backend:
          serviceName: pets-cat
          servicePort: 8080